{
  "name": "asan-invest",
  "private": true,
  "version": "0.1.0",
  "description": "ASAN INVEST — demo portal for investing in Azerbaijani government projects",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "test": "npm run test -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w client",
    "db:migrate": "npm run prisma:migrate -w server",
    "db:migrate:deploy": "npm run prisma:migrate:deploy -w server",
    "db:seed": "npm run prisma:seed -w server",
    "db:generate": "npm run prisma:generate -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
