# ASAN Invest client

Phase 1 React SPA (Vite + TypeScript + Tailwind). HTTP contract: [`docs/api.md`](../docs/api.md).

```bash
npm run dev:client
```

See the repository README for API and database setup. This app does not invent endpoints; it calls `/api/v1` as documented.
